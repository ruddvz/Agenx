# Cursor integration

Generated `.mdc` rule files. **Run convert first on a fresh clone:**

```bash
./scripts/convert.sh --tool cursor
cd /your/project && /path/to/agenx/scripts/install.sh --tool cursor
```

Rules install to `.cursor/rules/` in your project root.
