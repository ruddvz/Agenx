# MCP memory integration (planned)

Persistent memory between agent handoffs is described in `examples/workflow-with-memory.md`.

This integration is not yet bundled in the repo. Use any MCP server that supports store/recall semantics compatible with your tool, or continue with markdown handoffs via `planning/CHANGELOG.md` per client.

When implemented, this directory will document setup and agent prompt patches.
