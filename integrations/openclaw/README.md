# OpenClaw integration

```bash
./scripts/convert.sh --tool openclaw
./scripts/install.sh --tool openclaw
openclaw gateway restart   # if gateway already running
```

Workspaces install to `~/.openclaw/agency-agents/`.
