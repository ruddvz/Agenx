# Claude Code integration

Agents work natively from the repo — no conversion required.

```bash
./scripts/install.sh --tool claude-code
```

Per-client install (voice + stack + roster):

```bash
./scripts/agency-install.sh --client bookphysio-in --tool claude-code
```

Agents install to `~/.claude/agents/`.
